在webpack配置文件中，使用webpack的
optimization: {
    minimize: true // 压缩JS,由原来的6.9K --> 2.0K
}
// 建议直接去官方文档去查找就行


打包工具的核心作用
    1.代码的合并，打包
       对于使用commonJS模块化的东西，如果不合并，不打包，前端根本没有办法识别
       所以webpack最核心的功能就是它能对模块化的东西，进行合并，打包。
       
    2.压缩
       不压缩的话，JS文件体积比较大，不利于性能优化。
