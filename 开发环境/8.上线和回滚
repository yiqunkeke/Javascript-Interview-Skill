. 不会有具体的问题，交流询问的方式

知识点
    (1) 上线和回滚的基本流程
        .是非常重要的开发环节
         作为一个项目的参与者，你如果参与不了上线这个环节或者出问题后回滚环节，
         那么你基本就是这个项目开发的附属品
        .各个公司的具体流程不同
        .由专门的工具或者系统完成，我们无需关心细节。
        .如果你没有参与过，面试时也要说出要点 
        .只讲要点，具体实现无法讲解

        .上线流程要点
            .将测试完成的代码提交到git版本库的master分支
            .将当前服务器的代码全部打包并记录版本号，备份 1.0
            .将master分支的代码提交覆盖到线上服务器，生成新版本号 1.1

        .回滚流程要点   
            .将当前服务器的代码打包并记录版本号，备份 1.1
            .将备份的上一个版本号 1.0 解压，覆盖到线上的服务器，并生成新的版本号 1.2 

        备份和版本号记录，是上线和回滚的基础    

    (2) linux基本命令  
        .服务器使用linux居多，server版，只有命令行
        .测试环境要匹配线上环境，因此也是linux
        .经常登录测试机来自己配置、获取数据

      ----阿里云服务器 多试试，多操作
